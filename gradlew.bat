@rem Gradle wrapper launcher script (Windows).
@rem Generate gradle-wrapper.jar terlebih dulu — lihat gradlew untuk detail.
@echo off
set DIR=%~dp0
java -jar "%DIR%gradle\wrapper\gradle-wrapper.jar" %*
